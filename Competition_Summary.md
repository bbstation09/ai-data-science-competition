
# 한국타이어 FEM 기반 타이어 불량률 예측 경진대회 요약

본 문서는 대회 참가자가 반드시 이해해야 할 **전체 구조**, **Task1/Task2 목적**,  
**데이터 설명**, **평가 방식**, **의사결정 규칙** 등을 종합적으로 정리한 안내 자료입니다.

---

# 🏆 1. 대회 개요

본 대회는 **유한요소법(FEM) 기반 타이어 시뮬레이션 데이터**를 활용해  
실제 타이어 시험 생산 시 발생할 수 있는 **불량률을 정량적으로 예측**하고,  
이를 바탕으로 **최적의 시험 생산 대상을 선택**하는 AI 모델을 만드는 것이 목표입니다.

특징:
- 실제 산업 제조 데이터를 기반으로 함  
- 고차원 FEM 데이터(256개 지점 × x,y,p) 포함  
- 공정 파라미터, 설계 스펙 등 다양한 구조적 피처 제공  
- 불량(107) / 양품(613)의 심한 불균형 데이터  
- Task1(예측) + Task2(의사결정) 모두 해결해야 함  
- 최종 점수는 두 Task의 조합으로 결정됨 (기하평균)

---

# 🎯 2. Task1: 타이어 불량률 예측

## ■ 목적
타이어 설계 스펙 + FEM 시뮬레이션 결과를 바탕으로  
**불량(NG) 발생 확률을 예측**하는 분류 모델을 만드는 것.

## ■ 예측 대상
- Class = Good(0) 또는 NG(1)
- 제출 시 NG 확률(probability)을 제출해야 함.

## ■ 입력 데이터 구조
| 구분 | 설명 |
|------|------|
| 설계 스펙 | Mass_Pilot, Width, Aspect, Inch |
| 공정 파라미터 | Proc_Param1 ~ Proc_Param11 |
| 위치 벡터 | X1~X5, Y1~Y5 |
| FEM 결과 | x0~x255, y0~y255, p0~p255 |
| FEM 통계 | G1~G4 |

## ■ 학습용 데이터
- train.csv : 720행 × 799컬럼  
- 798개의 feature + Class 라벨

## ■ 평가 방식
**ROC-AUC**  
- NG 확률에 대한 AUC  
- 0.5 이하 = 무효, 1.0 = 완전 예측

---

# 🎯 3. Task2: 최적의 시험 생산 의사결정

## ■ 목적
Task1 모델이 예측한 NG 확률(= 불량 확률)을 기반으로  
**시험 생산할 설계안 200개 이하를 선택**하여  
**예상 이익(Net Profit)** 을 최대화하는 것이 목표.

즉, "어떤 타이어를 실제 시험 생산할 것인가?"를 결정하는 Optimization 문제.

## ■ 선택 시 비용 구조

| 실제 | 선택(True) | 보상 |
|------|------------|------|
| Good | 선택 | +100 |
| NG | 선택 | -2000 |
| 둘 다 | 선택❌ | 0 |

## ■ 제약 조건
- **선택 개수 ≤ 200**
- 200개 초과 시 **-99,999 페널티(사실상 실격)**

## ■ 손익 공식
\[
NetProfit = 100\sum I(d_i=True,y_i=Good) - 2000\sum I(d_i=True,y_i=NG)
\]

## ■ 최종 Task2 Score 변환
0~20,000 범위로 정규화하여 점수 반영.

---

# 🧮 4. 최종 종합 점수 계산 방식

최종 점수는 Task1(AUC)와 Task2(Net Profit)를 다음과 같이 조합한 기하평균:

\[
Total =
\sqrt{
rac{\max(	ext{AUC}-0.5, 0)}{0.5} 	imes
rac{\max(	ext{NetProfit}, 0)}{20000}
}
\]

→ 즉, 두 Task 모두 잘해야 높은 점수를 얻을 수 있음.

---

# 🗂 5. 제출 형식

sample_submission.csv 형식 준수:

- 총 932행 (test 466개 × 2개 row씩)
- ID_N_L, ID_N_P 모두 동일한 확률 값 필요  
- probability = Task1에서 예측한 NG 확률  
- decision = Task2 선택 여부(True/False)

예시:
```
ID_0001_L, ID_0001_P, probability, decision
ID_0001, ID_0001, 0.03421, True
ID_0001, ID_0001, 0.03421, True
```

---

# 📦 6. 데이터 파일 구성

| 파일명 | 설명 |
|--------|------|
| train.csv | 학습 데이터(라벨 포함) |
| test.csv | 테스트 데이터(라벨 없음) |
| sample_submission.csv | 제출 예시 |

---

# 🧠 7. 대회 참가자에게 중요한 핵심 포인트

- FEM 시뮬레이션 데이터는 타이어 내부의 물리적 상태를 반영하므로 매우 중요  
- 불균형 데이터이므로 적절한 처리 필요 (class weight, focal loss 등)  
- Task2에서는 **NG 확률이 낮은 설계안** 위주로 200개만 선택  
- 200개 초과하면 점수 0점(마이너스)이라 매우 중요  
- final score는 두 task의 결합이라 균형이 필요  
- test 데이터는 비/준지도 학습 가능 (하지만 실제 라벨을 확보해 학습하면 실격)

---

# ✔️ 8. 최종 요약

이 대회는 단순 분류 문제가 아니라  
**예측 + 의사결정 최적화**가 결합된 실제 제조공정 기반 산업 AI 문제다.

참가자는 다음을 수행해야 한다:
1) Task1: NG 확률 정확히 예측  
2) Task2: 예측값 + 비용 구조 기반으로 시험생산 200개 최적 선택  
3) 두 결과를 하나의 제출파일로 병합하여 제출  

이 문서는 위 과정을 이해하는 데 필요한 모든 정보를 정리한다.
